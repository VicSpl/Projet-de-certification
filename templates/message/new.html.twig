{% extends 'base.html.twig' %}

{% block title %}New Message
{% endblock %}

{% block body %}
	<h1>Create new Message</h1>

	{{ include('message/_form.html.twig') }}

	<a href="{{ path('recieve_index') }}">back to list</a>
	<a href="{{ path('sent_index') }}">back to list</a>
{% endblock %}
